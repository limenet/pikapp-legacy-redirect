{
  "devDependencies": {
    "eslint": "^6.7.2",
    "eslint-plugin-jest": "^23.1.1",
    "jest": "^24.9.0",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "cross-env": "^6.0.3",
    "express": "^4.17.1",
    "vhost": "^3.0.2"
  },
  "scripts": {
    "test": "cross-env NODE_ENV=production jest --force-exit --collect-coverage"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
